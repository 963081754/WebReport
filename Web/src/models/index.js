
import { models } from 'custompackages'

const { 
    Chinesize,
    Field,
    ChainType,
    Chain,
    SqlChain,
    Category,
    Db,
    Link,
    Report,
    ServerReport,
    Column,
    DemandBase,
    Demand,
    CombiDemand,
    NewCombiDemandItemFromDemand,

    Task,
    TreeItem,
    Manager,

    Role,
    User,
    Setting,
    UserType,
    deepCopy,
    SetType,
    SetWidth,
    SetAlign,
    // SetShape, // 后端用的
    // resetValue,
    // getClientReport,  // 前端 不用，要删。
    // DataAttrs, // 前端不需要
    likes,
    compareChar,
    equalWay,
    BaseDataTypes,
    BaseJsDataTypes 
} = models

export {
    Chinesize,
    Field,
    ChainType,
    Chain,
    SqlChain,
    Category,
    Db,
    Link,
    Report,
    ServerReport,
    Column,
    DemandBase,
    Demand,
    CombiDemand,
    NewCombiDemandItemFromDemand,

    Task,
    TreeItem,
    Manager,

    Role,
    User,
    Setting,
    UserType,
    deepCopy,
    SetType,
    SetWidth,
    SetAlign,
    // SetShape, // 后端用的
    // resetValue,
    // getClientReport,  // 前端 不用，要删。
    // DataAttrs, // 前端不需要
    likes,
    compareChar,
    equalWay,
    BaseDataTypes,
    BaseJsDataTypes
}